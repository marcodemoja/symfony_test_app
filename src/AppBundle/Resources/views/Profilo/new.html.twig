{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Add new profilo</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <div class="status"></div>
    {{ form_end(form) }}


{% endblock %}